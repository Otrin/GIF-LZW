#ifndef GLOBALTABLECOMPARISON_H
#define GLOBALTABLECOMPARISON_H

#include "gif.h"


/**
 * @brief GlobalTableComparison compares the efficiency of the LZW-Algorithm woth respect to file size and runtime.
 *
 */
class GlobalTableComparison
{
public:
    /**
     * @brief Constructor to create a new GlobalTableComparison object.
     *
     */
    GlobalTableComparison(Gif p_gif, bool p_global);

    /**
     * @brief This amazing method does stuff!
     *
     * @param glorious_input - The glorious_input gets put in gloriously.
     * @return int - glorious output!
     */
    int dosomeotherstuff(int glorious_input);
};

#endif // GLOBALTABLECOMPARISON_H
